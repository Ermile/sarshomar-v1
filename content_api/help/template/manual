METHOD		URL 						HEADER 								BODY
# ------------------------------------------ POLL
POST		api/poll 					[application/json]					[args]
GET 		api/poll?id=[id]
PUT			api/poll 														[id]
DELETE		api/poll?id=[id] 									[id]

# ------------------------------------------ FILE
POST		api/file
GET 		api/file?id=[id]

# ------------------------------------------ ANSWER
POST		api/answer
GET 		api/answer?id=[id]
PUT			api/answer 														[id]

# ------------------------------------------ CONTACT
POST		api/contact

# ------------------------------------------ SEARCH
GET 	 	api/search?[search condition]
POST 	 	api/search 														[search condition]

# ------------------------------------------ ASK
GET 		api/ask 					[type: random|null]

-------------------------------------------this-is-okay------------------------------------------------
METHOD		URL 						HEADER 								BODY
# ------------------------------------------ POLL
POST		api/addPoll 					[application/json]				[args]

GET 		api/getPoll?{id=[id]|?type=last|random}
POST 		api/getPoll														[id]

POST		api/editPoll 													[id]

POST		api/removePoll 													[id]

# ------------------------------------------ FILE
POST		api/addFile

GET 		api/getFile?id=[id]
POST		api/getFile														[id]


# ------------------------------------------ ANSWER
POST		api/addAnswer

GET 		api/getAnswer?id=[id]
POST		api/getAnswer													[id]

PUT			api/editAnswer 													[id]
POST		api/editAnswer													[id]


# ------------------------------------------ CONTACT
POST		api/sendFeedback

# ------------------------------------------ SEARCH

GET 	 	api/search?[search condition]
POST 	 	api/search 														[search condition]

# ------------------------------------------ TRANSACTIONS
POST 		api/calcPrice
GET 		api/calcPrice

# ------------------------------------------ FAVORITES
GET 		api/getFavorites?user=[id]
POST 		api/getFavorites

POST 		api/addFavorites


# ------------------------------------------ LIKE
GET 		api/getLike?user=[id]
POST 		api/getLike

POST 		api/addLike


